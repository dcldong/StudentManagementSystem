create database students;
use  students;
CREATE TABLE student (
    id int(5) primary key,
    name varchar(50),
    department date,
    major date,
    grade int(5),
    classes varchar(50),
    age int(5)
);